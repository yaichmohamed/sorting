InsertionSort(A)
1. for i = 1 to length(A) - 1 do   //Iterate over the array starting from the second element
2.    j = i   //Store the current index in a variable
3.    while j > 0 and A[j] < A[j-1] do   //Continue swapping elements until the current element is in its correct position
4.        swap A[j] and A[j-1]   //Swap the current element with the previous element
5.        j = j - 1   //Move the index to the left
6.    end while
7. end for
